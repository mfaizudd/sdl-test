project('sdl-test', 'cpp')
sdl2_dep = dependency('sdl2')
includes = include_directories(['src'])
sources = [
  'src/main.cpp'
]
executable('sdl-test', sources, dependencies: sdl2_dep, include_directories: includes)
